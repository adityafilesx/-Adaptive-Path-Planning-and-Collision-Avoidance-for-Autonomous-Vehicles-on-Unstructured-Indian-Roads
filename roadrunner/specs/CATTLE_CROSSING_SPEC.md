# Sudden Cattle Crossing — optional target

Future, lower-priority visual target for Phase 8 actor ID 701 at [70,16], velocity [0,-2] m/s, canonical class `cattle`. A generic movable-object proxy is acceptable if no cattle asset exists; it must retain cattle dimensions and risk class. Use `cattleCrossing.json`; author only after both primary scenes validate.
